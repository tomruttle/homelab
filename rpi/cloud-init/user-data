#cloud-config

hostname: rpi

ssh_pwauth: false

users:
  - name: tom
    groups: [sudo]
    shell: "/bin/bash"
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_import_id:
      - gh:tomruttle
    lock_passwd: true

mounts:
  - [sda, /opt/data]